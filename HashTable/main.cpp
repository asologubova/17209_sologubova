#include "hashTable.h"
//#include <ctime>
//#include <vector>
//#include <string>
//#include <iostream>
//
//
//void generateRandomString(std::string & s, size_t length) {
//	for (size_t i = 0; i < length; i++) {
//		s.push_back(rand() % 127);
//	}
//}
//
//void generateRandomTable(HashTable & t, std::vector<std::string> & names) {
//	for (size_t i = 0; i < names.size(); i++) {
//		generateRandomString(names.at(i), 5 + i);
//
//		size_t a = i * 2 + 20;
//		size_t w = i * 3 + 30;
//		Value v(a, w);
//
//		t.insert(names.at(i), v);
//
//	}
//}

int main(int argc, char * argv[]) {
	//srand(static_cast<size_t>(time(0)));
	//size_t N = 10;
	//HashTable ht1;
	//std::vector<std::string> names(N);
	//generateRandomTable(ht1, names);
	//HashTable ht2(ht1);
	//HashTable ht3 = ht1;

	//std::string str;
	//generateRandomString(str, 15);
	//while (ht2.contains(str))
	//	generateRandomString(str, 15);
	//Value v(12, 15);
	//ht2.insert(str, v);

	//ht2 = ht1; 
	//if (ht1 == ht2)
	//ht2.erase(names[5]);
	//EXPECT_TRUE(ht1 != ht2);


	return 0;
}